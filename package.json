{
  "name": "prex-module",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "tsc -b && vite build",
    "watch": "vite build --watch",
    "build:capture": "deno task build:capture",
    "build:all": "npm run build && deno task build:capture"
  },
  "dependencies": {
    "preact": "^10.29.8"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.6",
    "@types/node": "^24.13.3",
    "typescript": "~6.0.2",
    "vite": "^8.2.2"
  }
}
